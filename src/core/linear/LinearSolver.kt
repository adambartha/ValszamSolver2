package core.linear

class LinearSolver
{
    private var root: LinearNode? = null
    fun solve(input: String): Double
    {
        // TODO, TEST (5+X^2)+3*Y^2+7
        var buffer = ""
        for(char in input)
        {
            buffer += char
        }
        return 0.0
    }
}
